import sys
import os

# Adicione o diretório do seu aplicativo ao PATH
sys.path.insert(0, '/home/ubuntu/playlist_creator_app')

from app import app as application  # Substitua 'app' pelo nome do seu arquivo Python
